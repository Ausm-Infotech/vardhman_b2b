import 'package:flutter/material.dart';

class PaidView extends StatelessWidget {
  const PaidView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Paid View'),
    );
  }
}
