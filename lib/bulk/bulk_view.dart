import 'package:flutter/material.dart';

class BulkView extends StatelessWidget {
  const BulkView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Bulk View'),
    );
  }
}
